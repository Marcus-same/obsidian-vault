---
title: "📅 本周排期 - W<% tp.date.now("WW") %>"
type: weekly-view
week_start: <% tp.date.now("YYYY-MM-DD", 1 - tp.date.now("d")) %>
week_end: <% tp.date.now("YYYY-MM-DD", 7 - tp.date.now("d")) %>
tags: [weekly, schedule]
---

# 📅 本周排期

> <% tp.date.now("YYYY 年 M 月 d 日", 1 - tp.date.now("d")) %>（一） ~ <% tp.date.now("YYYY 年 M 月 d 日", 7 - tp.date.now("d")) %>（日）
> 第 <% tp.date.now("WW") %> 周

---

<%* const monday = tp.date.now("YYYY-MM-DD", 1 - tp.date.now("d")); /* 本周一 */ 
   const weekdays = ["一", "二", "三", "四", "五", "六", "日"];
   for (let i = 0; i < 7; i++) {
     const day = new Date(monday);
     day.setDate(day.getDate() + i);
     const dateStr = day.toISOString().slice(0, 10);
     const monthDay = `${day.getMonth()+1}月${day.getDate()}日`; 
     const weekday = weekdays[i];
_%>
## <% monthDay %>（<%- weekday %>）

```dataview
TASK
FROM "20-Actions"
WHERE !completed AND deadline = date("<% dateStr %>")
SORT priority DESC
```

<%* } _%>

---

> 📌 调整行动卡日期：直接修改该行动卡 `scheduled_date` 字段即可重新排期
