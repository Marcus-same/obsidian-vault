---
date: <% tp.date.now("YYYY-MM-DD") %>
tags: [daily]
---

# <% tp.date.now("YYYY-MM-DD (dddd)") %>

<!-- 在今日任务写任务时，如果对应 20-Actions 里的行动卡，用 [[20-Actions/文件名|显示名]] -->
<!-- 完成后按 Ctrl+Shift+S 同步状态到行动卡 -->

## 📋 今日任务

```dataview
TASK
FROM ""
WHERE file.path = this.file.path AND !completed
SORT section ASC
```

## 📥 收件箱待处理

```dataview
TABLE file.cday as 收集日
FROM "00-Inbox"
WHERE file.cday < date(today) AND file.cday >= date(today) - dur(7 days)
SORT file.cday DESC
```

## ⏳ 待行动

```dataview
TASK
FROM "20-Actions"
WHERE !completed AND deadline = date(today)
GROUP BY file.link
```

```dataview
TASK
FROM "20-Actions"
WHERE !completed AND deadline < date(today)
GROUP BY file.link
```

## 📌 滚动待办
```dataview
TASK
FROM "01-Daily"
WHERE !completed AND file.day < date(today)
GROUP BY file.link
```

## 📝 工作记录

## 🤔 思考 & 复盘

## 📎 随手记
