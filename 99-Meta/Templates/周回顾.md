---
title: "周回顾 - W<% tp.date.now("ww", 0) %>"
type: weekly
week_start: <% tp.date.now("YYYY-MM-DD", 0) %>
week_end: <% tp.date.now("YYYY-MM-DD", 6) %>
tags: [weekly, review]
---

# 📅 周回顾：<% tp.date.now("YYYY") %> 年第 <% tp.date.now("ww", 0) %> 周

> <% tp.date.now("YYYY-MM-DD", 0) %>（一） ~ <% tp.date.now("YYYY-MM-DD", 6) %>（日）

---

## ✅ 本周完成

```dataview
TASK
FROM "20-Actions"
WHERE completed AND completion-date >= date("<% tp.date.now("YYYY-MM-DD", 0) %>") AND completion-date <= date("<% tp.date.now("YYYY-MM-DD", 6) %>")
GROUP BY file.link
```

## 📋 本周新增待办

```dataview
TASK
FROM "20-Actions"
WHERE !completed AND file.ctime >= date("<% tp.date.now("YYYY-MM-DD", 0) %>")
GROUP BY file.link
```

## 🔄 滚动未完成

```dataview
TASK
FROM "20-Actions"
WHERE !completed AND file.ctime < date("<% tp.date.now("YYYY-MM-DD", 0) %>")
GROUP BY file.link
```

## 📥 本周收件箱整理

```dataview
TABLE file.ctime as 创建时间
FROM "00-Inbox"
WHERE file.ctime >= date("<% tp.date.now("YYYY-MM-DD", 0) %>")
SORT file.ctime DESC
```

## 🧠 本周 Wiki 更新

```dataview
TABLE date as 更新日期
FROM "10-Wiki"
WHERE date >= date("<% tp.date.now("YYYY-MM-DD", 0) %>")
SORT date DESC
```

---

## 📝 本周复盘

### 亮点 ✨

- 

### 不足 ⚠️

- 

### 下周重点 🎯

- [ ] 

## 📎 随手记

