---
title: 📅 本周排期
type: weekly-view
tags: [weekly, schedule]
---

# 📅 本周排期

> `= dateformat(date(today), "yyyy 年第 W 周  M 月 d 日 EEEE", "zh")`

---

## 今天 · `= dateformat(date(today), "M 月 d 日 EEEE", "zh")`

```dataview
TASK
FROM "20-Actions"
WHERE !completed AND deadline = date(today)
SORT priority DESC
```

## 本周行动卡

```dataview
TABLE deadline as 日期, priority as 优先级, status as 状态
FROM "20-Actions"
WHERE !completed
  AND deadline >= date(today) - dur(1 day)
  AND deadline <= date(today) + dur(7 days)
SORT deadline ASC
```

## ⏰ 逾期待办

```dataview
TABLE deadline as 截止日期, priority as 优先级
FROM "20-Actions"
WHERE !completed AND deadline < date(today)
SORT deadline ASC
```

---

> 📌 排期调整：修改行动卡 `scheduled_date` 字段即可
