---
title: 🏠 工作台
type: dashboard
date: 2026-07-16
tags: [dashboard, index]
---

# 🏠 工作台

> 📅 `= dateformat(date(2026-07-16), "yyyy 年 M 月 d 日 EEEE", "zh")` · 第 `= dateformat(date(2026-07-16), "W", "zh")` 周

---

## 🔴 紧急关注

```dataview
TABLE deadline as 截止日期, priority as 优先级
FROM "20-Actions"
WHERE !completed AND deadline <= date(2026-07-18)
SORT deadline ASC
```

## 📅 本周行动卡

```dataview
TABLE deadline as 截止日期, priority as 优先级, status as 状态
FROM "20-Actions"
WHERE !completed AND deadline >= date(2026-07-13) AND deadline <= date(2026-07-19)
SORT deadline ASC
```

## 📥 收件箱待处理

```dataview
TABLE file.cday as 收集日, source_type as 类型
FROM "00-Inbox"
WHERE file.name != "README" AND file.name != "🏠 首页"
SORT file.cday DESC
```

## 🧠 最新 Wiki

```dataview
TABLE date as 整理日期, status as 状态
FROM "10-Wiki"
WHERE file.name != "README" AND file.name != "index" AND file.name != "log"
SORT date DESC
LIMIT 5
```

## 📝 本周日记

```dataview
LIST
FROM "01-Daily"
WHERE file.day >= date(2026-07-13) AND file.day <= date(2026-07-19)
SORT file.day ASC
```

---

## ⚡ 快捷操作

- 📝 **写日记**：`01-Daily/` 按日期命名
- 📥 **快速捕获**：按 `Ctrl+P` → `Templater: Quick capture`
- 🧠 **整理收件箱**：对我说 `"整理收件箱"`
- 📅 **周回顾**：对我说 `"做周回顾"`
- ⏳ **检视进度**：对我说 `"检查待办"`
