---
title: 🏠 工作台
cssclasses: dashboard
date: 2026-07-16
---

# 🏠 工作台

> `= dateformat(date(today), "yyyy 年 M 月 d 日 · EEEE · 第 W 周", "zh")`

---

## 🔴 近期待办

```dataview
TABLE deadline as 截止, priority as 优
FROM "20-Actions"
WHERE !completed AND deadline >= date(today) AND deadline <= date(today) + dur(3 days)
SORT deadline ASC
```

## 📅 本周计划

```dataview
TABLE deadline as 截止, priority as 优
FROM "20-Actions"
WHERE !completed AND deadline > date(today) + dur(3 days) AND deadline <= date(today) + dur(7 days)
SORT deadline ASC
```

## ⏰ 逾期待办

```dataview
TABLE deadline as 截止, priority as 优
FROM "20-Actions"
WHERE !completed AND deadline < date(today)
SORT deadline ASC
```

---

## 📥 收件箱

```dataview
TABLE file.cday as 收集日
FROM "00-Inbox"
WHERE file.name != "README" AND file.name != "🏠 首页"
SORT file.cday DESC
```

## 🧠 最新 Wiki

```dataview
TABLE date as 整理日, status as 状态
FROM "10-Wiki"
WHERE file.name != "README" AND file.name != "index" AND file.name != "log"
SORT date DESC
LIMIT 5
```

---

## ⚡ 快捷操作

→ 📝 说 **"今天简报"** — 查看今日待办汇总
→ 📥 说 **"整理收件箱"** — 批量处理收件箱
→ 📅 说 **"做周回顾"** — 自动生成周回顾
→ ⏳ 说 **"检查待办"** — 查看所有待办状态
